import { createUseStyles } from "react-jss";

export default createUseStyles({
  colorPicker: {
    margin: "1rem",
    padding: 0,
    width: "50px",
    height: "50px",
    border: "none",
    outline: "0 none",
    background: "transparent",
    cursor: "pointer",
  },
});
